services:

  database:
    image: postgis/postgis
    ports: 
      - 5432:5432
    env_file:
      - .env
    volumes:
      - ./db-data/:/var/lib/postgresql/data/
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql

volumes:
  db_data:
