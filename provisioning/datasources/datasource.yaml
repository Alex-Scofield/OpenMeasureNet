apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    uid: postgres_main
    url: database:5432
    database: dev_db
    user: pg
    secureJsonData:
      password: dev_pass
    jsonData:
      sslmode: disable
      postgresVersion: 1500
      database: dev_db
